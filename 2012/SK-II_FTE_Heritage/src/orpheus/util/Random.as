package orpheus.util{	/**	 * MZ Package 1.0	 * @author INEX	 */	public class Random	{		/**		 * 주소에서 파일명을 제외한 디렉토리를 반환합니다.		 */		public static function getRandomList(pList:Array):Array		{			var listNum:int = pList.length;			var kList:Array = [];			for (var i:uint = 0; i < listNum; i++)			{				var kRandom:int = int(Math.random() * pList.length);				var kOne:Object = pList[kRandom];				kList.push(kOne);				pList.splice(kRandom, 1);			}			return kList;		}	}}